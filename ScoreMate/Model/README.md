# Model

This folder contains the app's domain models.

These models represent the data the UI works with, such as leagues, teams, matches, and dates. Keeping them separate from raw API response types helps the app stay cleaner and easier to maintain.
