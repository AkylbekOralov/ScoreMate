# UI

This folder contains shared UI building blocks and design values used across the app.

It includes:

- spacing, radius, and font constants
- shared asset access helpers
- reusable UI utilities
- the `Components` folder for common SwiftUI views

Centralizing these pieces helps keep the visual system consistent.
